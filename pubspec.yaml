name: skynet_send
description: An absolute bare-bones web app.
# version: 1.0.0
#homepage: https://www.example.com
environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  cryptography:
    git:
      url: https://github.com/CDDelta/cryptography/
      ref: web-key-fix
      path: cryptography
  http: ^0.12.2
  
dev_dependencies:
  build_runner: ^1.10.0
  build_web_compilers: ^2.11.0
  pedantic: ^1.9.0
